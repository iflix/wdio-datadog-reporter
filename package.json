{
  "name": "@iflix/wdio-datadog-reporter",
  "version": "0.0.9",
  "description": "A WebdriverIO plugin to report into Datadog.",
  "main": "dist/reporter.js",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "eslint src test",
    "build": "cross-env BABEL_ENV=commonjs babel src --out-dir dist",
    "prepublish": "npm run build"
  },
  "files": [
    "dist/"
  ],
  "bug": [
    "https://teamiflix.atlassian.net/projects/QA/issues/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iflix-letsplay/wdio-datadog-reporter.git"
  },
  "keywords": [
    "iflix",
    "wdio",
    "reporter",
    "datadog"
  ],
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.14.0",
    "babel-eslint": "^7.0.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-2": "^6.13.0",
    "cross-env": "^3.0.0",
    "eslint": "^3.6.0",
    "eslint-plugin-import": "^2.0.0",
    "rimraf": "^2.5.4",
    "standard": "^8.6.0"
  },
  "dependencies": {
    "dogapi": "^2.6.0"
  }
}
